# SMC License System - Environment Configuration Template
# ========================================================
# 
# This file contains environment variables required for the SMC License System.
# Copy this file to .env and fill in the values.
#
# IMPORTANT SECURITY NOTES:
# - Never commit .env files to version control
# - Keep the SHARED_SECRET_KEY confidential
# - Both license.lic file and .env are required for activation
#

# ===========================================
# SMC LICENSE SYSTEM CONFIGURATION
# ===========================================


# License File Path (relative to application root)
# Default: license.lic (place in same directory as the executable)
SMC_LICENSE_FILE_PATH=license.lic

# ESP32 Default IP Address for MAC validation
# Used when connecting to ESP32 device for hardware binding validation
SMC_ESP32_DEFAULT_IP=192.168.4.1

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================

# Application Environment
# Options: development, production, test
; NODE_ENV=production
NODE_ENV=development

# Device Type (build-time configuration)
# Options: DS12, DS16
DEVICE_TYPE=DS12

# ===========================================
# DATABASE & HARDWARE CONFIGURATION
# ===========================================

# Maximum log records to keep in database
MAX_LOG_COUNTS=500

# Maximum number of users allowed
MAX_USER=20

# Hardware communication settings
KU_BAUDRATE=19200
KU_PORT=auto
INDI_PORT=auto
INDI_BAUDRATE=115200

# System service code
SERVICE_CODE=SMC2025


# ===========================================
# PHASE 4.2: PRODUCTION LICENSE & WIFI MANAGEMENT
# ===========================================

# License Bypass Mode (Development Only)
# true = Skip all license validation (BLOCKS PRODUCTION BUILD)
# false = Full license validation required
SMC_LICENSE_BYPASS_MODE=false

# Platform WiFi Strategy
# true = Auto WiFi connection attempt (Windows compatible)
# false = Manual WiFi connection instructions (macOS compatible)
SMC_PLATFORM_WIFI_AUTO=false

# Development Real Hardware Mode  
# true = Connect to real ESP32 in development (no mock MAC)
# false = Standard development behavior
SMC_DEV_REAL_HARDWARE=true

# ===========================================
# INSTRUCTIONS
# ===========================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Generate shared key using CLI tool:
#    smc-license show-key
#
# 3. Update SHARED_SECRET_KEY with the key from step 2
#
# 4. Place your license.lic file in the application root directory
#
# 5. Update other configuration values as needed for your environment
#
# 6. Never commit .env file to version control:
#    Add .env to your .gitignore file
#
# For more information, see CLI documentation: cli/README.md
#