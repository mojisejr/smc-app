import { DB, IO } from "../enums/ipc.enums";
import { ipcMain } from "electron";

export function logEvents() {
  // Event listener count logging removed for production
  // This was used for debugging IPC event registration
}
