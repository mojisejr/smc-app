appId: com.deprecision.smartdrugcart
productName: SmartDrugCart
copyright: Copyright © 2023 Deprecision
directories:
  output: dist
  buildResources: resources
files:
  - from: .
    filter:
      - package.json
      - app
# Environment variable injection for license bypass support
env:
  SMC_LICENSE_BYPASS_MODE: ${SMC_LICENSE_BYPASS_MODE}
  BUILD_TYPE: ${BUILD_TYPE}
  SMC_DEV_REAL_HARDWARE: ${SMC_DEV_REAL_HARDWARE}
  NODE_ENV: ${NODE_ENV}
publish: null
